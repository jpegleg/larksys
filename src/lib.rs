#[cfg(test)]
mod tests {
    #[test]
    fn placeholder () {
      let sordu = "Execute each function and check that it did what we expect.";
      assert_eq!(sordu,"Execute each function and check that it did what we expect.");
    }
}
